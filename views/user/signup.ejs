<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/signup.css">

    <script src="https://kit.fontawesome.com/0a256d9d04.js" crossorigin="anonymous"></script>

    <title>Wine ONE LIFE - Login</title>
</head>

<body>

    

    <div class="bg-img">
        <form class="container" method="POST" action="/user/signup" enctype="multipart/form-data">

            <div class="login-sign">
                <div class="ingresa"><a href="/user">Ingresá</a></div>
                <div class="registra"> <a href="/user/signup">Registrarse</a> </div>
            </div><br>

            <label for="image">Imagen de perfil:</label>
            <input type="file" id="image" name="image" accept="image/*">
            <br>
            <br>
            <label for="firstName"><b>Nombre:</b></label>
            <input type="text" name="firstName" id="firstName" value="<%= locals.oldData ? oldData.firstName : null %>" placeholder="Nombre" />
            <% if (locals.errors && errors.firstName) { %>
            <div class="form-error"><%= errors.firstName.msg %></div><br>
            <% } %>

            <label for="lastName"><b>Apellido:</b></label>
            <input type="text" name="lastName" id="lastName" value="<%= locals.oldData ? oldData.lastName : null %>" placeholder="Apellido" />
            <% if (locals.errors && errors.lastName) { %>
                <div class="form-error"><%= errors.lastName.msg %></div><br>
                <% } %>

            <label for="email"><b>Correo Electrónico</b></label>
            <input type="email" placeholder="Tu Email" name="email" id="lastName" value="<%= locals.oldData ? oldData.email : null %>">
            <% if (locals.errors && errors.email) { %>
                <div class="form-error"><%= errors.email.msg %></div><br>
                <% } %>

            <label for="password"><b>Contraseña</b></label>
            <input type="password" placeholder="Constraseña" name="password" id="password" value="">
            <% if (locals.errors && errors.password) { %>
                <div class="form-error"><%= errors.password.msg %></div><br>
                <% } %>



            <label for="check-password"><b>Confirmá tu contraseña:</b></label>
            <input type="password" name="check-password" id="check-password" value="" placeholder="Confirmar contraseña" />
 

            <div class="boton">
                <button type="submit" class="btn">Crear usuario</button>
            </div>

        </form>
    </div>
</body>

</html>